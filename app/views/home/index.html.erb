
	<h1 class="custom-h1 col-xs-4 col-xs-offset-4 see-box text-center">Menu</h1>
	<% @posts.each do |post| %>
	<div class="col-xs-10 col-xs-offset-1 col-sm-4 col-sm-offset-1 see-box text-center">
		<%= image_tag post.DL, :class => 'img-responsive' %>
		<h1>
		<%= post.title %>
	</h1>
	<%= post.description %>
	</div>
	<% end %>

